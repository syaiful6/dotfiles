Reference MCP definitions I usually use, symlink it to any project that needs it.

## Claude code:

The location of MCP file that Claude loaded automatically is on project root, the file must be name .mcp.json
