# Dotfiles

My precious stuff. Only I should use this.

I use custom scripts to manage and install these files. It basically has same behaviour
with GNU Stow, but with few differences. Mainly, it never symlinks whole folder, instead
it create symlinks for each file inside the folder.

Currently I use this on MacOS, Arch Linux and Ubuntu. The packages folder manage system
packages for each OS.

